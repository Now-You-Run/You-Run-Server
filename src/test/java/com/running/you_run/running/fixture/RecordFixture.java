package com.running.you_run.running.fixture;

import com.running.you_run.running.payload.request.RecordStoreRequest;

import java.time.LocalDateTime;

public class RecordFixture {
//    public static RecordStoreRequest createRecordRequest(Long userId, Long trackId, long distance, double resultTimeSeconds) {
//        LocalDateTime now = LocalDateTime.now();
//        LocalDateTime finished = now.plusSeconds((long) resultTimeSeconds);
//
//        return new RecordStoreRequest(
//                userId, "BOT", trackId, null, null,
//                now, finished, 0.0, distance
//        );
    }
}
